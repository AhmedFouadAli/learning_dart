void main() {
  // print(5 + 3);
  // print(5 - 3);
  // print(5 / 3);
  // print(5 * 3);
  // print(5 ~/ 3);
  // print(5 % 3);
  // print(5 % 2);
  // print(158 % 10);
  // () */% +-
  // int x = 10;
  // x = x + 5;
  // x *= 5;
  // x ~/= 5;
  // print(x);
  // int y = 20;
  // y = y + 1;
  // y += 1;
  // y++;
  // ++y;
  // int w = ++y;
  // print(y);
  // print(w);

  // int x = 1;
  // int y = x++; //1
  // int z = --y; // 0 , 0
  // print('x: $x, y: $y, z: $z');
  bool isCorrect = 5 == 3;
  isCorrect = 5 != 3;
  isCorrect = 5 <= 3;
  isCorrect = 5 < 3;
  isCorrect = 5 >= 3;
  isCorrect = 5 > 3;
  // && both must be true to be true;
  // || one must be true to be true;
//   int age = 15;
// //              condition ? is true take first : if false take second
//   String type = age >= 19 ? "adult" : "child";
//   print(type);

//   String email = "";
//   int x = 3;
//   print(email.isNotEmpty || x++ > 5);
//   print(x);
}
